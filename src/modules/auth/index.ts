/**
 * /src/modules/auth — public barrel
 * Exports only the contracts components should depend on.
 */
export { SessionGuard } from "./session-guard";
export { useAuthSession } from "./use-auth-session";
